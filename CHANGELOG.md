# 變更紀錄（Changelog）

本檔記錄每次發佈到 `main`（＝正式部署上線）的版本。版號規則見 [docs/versioning.md](docs/versioning.md)。
格式採語意化版號（SemVer）；每次部署另有自動附掛的 **commit SHA** 與 **建置時間**，可於「系統設定 → 關於」或畫面側邊欄查看，用來精準對回是哪一次部署。

## [1.8.0] - 2026-07-09

### 新增（員工生命週期：多段留停歷史 B-1）
- **留停／停職／暫離改用多段區間 `leaveRecords`**：每段各自的類別、生效日、復職日與給薪比例，支援同一員工多段歷史（例如兩次留停），破月計薪與停保/復保名冊皆逐段計算。主檔基本分頁下方「區間紀錄」逐段增刪編輯。
- 舊單段資料自動遷移（store v2→v3 回填），與既有資料及 TC-9 逐位元不變。

### 變更（正式站聚焦薪資作業）
- **「出勤打卡」與「專案」暫不啟用**（功能開關 `FEATURES`，全環境隱藏 UI 入口）：讓人事先專注薪資計算/結構/報表分析預測的驗收。**僅隱藏入口，不刪程式碼、資料與計算**——store、示範資料、相關純函數與元件全部保留，整合成熟後改一個開關即可重新啟用。薪資/保費/稅/報表數字不受影響。

### 修正
- **DataTable 十字對準破壞凍結首欄／黏性表頭**：被選到的黏性儲存格原本會變半透明、露出捲動到後面的欄位（看似「首欄凍結失效」）；十字色塊改用背景圖層（gradient）疊加、不覆蓋不透明底色，凍結/黏性儲存格維持不透明。全站表格同步修正。

### 相容性
- 純前端；驗收基準 TC-9（8 名員工合計）不受影響。多段留停為新增能力、舊資料自動相容。

## [1.7.0] - 2026-07-08

### 新增（統一設計語言 · 元件庫治理）
- **語意色彩 token 單一來源**：`success`／`warning`／`info`／`danger`＋「已變更」`edit`＋表格欄位 `col-input`／`col-formula`／`col-assumption`，定義於 `index.css` CSS 變數、映射於 `tailwind.config.js`。取代散落各處的 emerald/amber/sky/rose 硬編色——改一次全站同步。
- **圖表色票「Gem」寶石色票**（藍寶石/海藍寶石/祖母綠/黃玉/紅玉髓/粉晶/石榴石/紫水晶）：沿色相環排序，經色票驗證器實測通過亮度帶/彩度/CVD 色盲分離/對比四項檢查；`charts` 改讀 `--chart-1..8` CSS 變數。
- **暗黑模式**：`.dark` 經刻意 UI/UX 色彩設計（表面抬升層次、降飽和背景、WCAG 對比驗證）；**設定頁「公司與出勤 → 外觀」可切換**淺色/深色/跟隨系統（偏好存於裝置、不隨資料備份、首繪防閃爍）。
- **Callout 狀態提示框**元件（良好/警示/提醒/危險），取代各畫面手刻色塊。
- **稽核回復（F6）**：逐欄編輯（薪資/員工/眷屬/代扣稅）可於設定頁稽核表一鍵「回復」成變更前值；已確認月結期間擋下（須先取消確認），回復本身另記稽核。
- **DataTable**：Excel 式單欄篩選（文字多條件＋推薦、數值範圍、多選勾選）、十字對準、可調欄寬、門檻分頁（5/15/30/50）。
- **TabPills** 統一分頁列；全站表格/分頁改用共用元件。

### 變更
- **調薪核定寫回（F7）**：稽核摘要加計人數、月薪資總額 before→after、增減額與平均調幅。
- **views 硬編色全面改語意 token**（15 檔）：換色票主題或深色模式不再漂移。
- **Storybook 僅 dev 發佈**（開發工具不隨 stage/main 上線，簡化維護）。

### 相容性
- 純前端、無資料結構破壞；驗收基準 TC-9（8 名員工合計）不受影響。深色為使用者偏好、預設「跟隨系統」，不影響既有淺色體驗。

## [1.6.0] - 2026-07-08

### 新增（元件 UX：無障礙／觸控／跨裝置）
- **鍵盤與無障礙**：單選（radio）改為 WAI-ARIA `radiogroup`、多選（multiselect）為一組 `checkbox`；方向鍵移動焦點、Space/Enter 選定、**Esc 取消收合**；進編輯自動聚焦控制項；唯讀值與鎖定欄補 `aria-label`／`aria-disabled` 並可聚焦，讓螢幕閱讀器讀得到目前值與鎖定原因。
- **色盲友善**：選中的分段鈕／標籤片加**打勾 ✓ 圖示**（不只靠顏色區分）。
- **編輯可發現性**：唯讀欄位右側加**低調鉛筆圖示**（桌機 hover 加深、觸控恆微顯）。
- **提示泡泡強化**：可用鍵盤聚焦顯示（截斷內容 Tab 可讀）、**溢出視窗邊緣自動夾擠／上緣不足翻到下方**、長壓時不誤選字。

### 變更
- **觸控目標放大**：手機／平板（粗指標）裝置將分段鈕／標籤片／undo·redo／變更摘要還原／表格分頁鈕放大至 **≥44px**（WCAG 2.5.5），桌機維持精簡。
- **還原一致性**：變更摘要（ChangeSummary）逐欄與「全部還原」比照欄位 undo，**先以「現值→還原後」確認才執行**。
- **表單響應式**：基本資料表單於手機改為單欄（`grid-cols-1 sm:grid-cols-2`），薪資／眷屬進為 `sm:grid-cols-2 lg:grid-cols-3`，避免窄螢幕兩欄擠壓。

## [1.5.0] - 2026-07-08

### 新增
- **提示泡泡（Tooltip）**：桌機 hover、行動長壓顯示完整內容——套用於截斷的欄位值、多選預覽標籤、變更摘要、DataTable 截斷欄。
- **DataTable 欄位可截斷**（`truncate` 最大寬度＋提示）；**圖表（柏拉圖／長條）類別過多時只顯示前 N＋「其他（N 項）」彙總**，避免標籤糊成一團。

### 變更
- **多選收合**改為顯示**前 2 項＋「其他 N 項」**（固定長度、點擊展開全部），不再一次攤開所有選項。
- 送出前變更摘要（ChangeSummary）超過 8 筆時**內部捲動**。

## [1.4.1] - 2026-07-08

### 變更（壓力測試後的溢出處理）
- **單選 radio／多選 multiselect 改為預設只顯示目前值、點擊才展開選項**（多選以「完成」收合），避免選項過多時畫面凌亂。
- 欄位過長值**截斷顯示**（`…`＋hover 顯示完整）；`Stepper` 多步驟可橫向捲動。

## [1.4.0] - 2026-07-08

### 新增
- **欄位 undo / redo**：唯讀逐欄編輯的每欄可還原（undo），還原後可重做（redo）；**undo 與 redo 都會先以行內確認列出「舊值 → 新值」，確認才執行**。

### 變更
- 套用新欄位型態到實際表單：基本資料「稅務身分／每月扣繳方式／給薪比例」改用 **radio 分段選擇**。
- 當月異動加班／請假時數顯示單位 **h**。
- 薪資結算清單「實發」為負時以**紅字括號**（如 `(2,000)`）呈現，凸顯不應出現的負值。

## [1.3.0] - 2026-07-07

### 新增
- `EditableField` 新增欄位型態：**單選 radio（分段按鈕）**、**多選 multiselect（可切換標籤片）**；「是/否」改為**分段切換**呈現。

### 變更
- **數字顯示不四捨五入**（顯示實際儲存值，進位交由計算端，避免顯示與實際不符）。
- 數字單位以符號/英文呈現：金額加 **$**、比率 **%**、一般數字可帶**單位**（如 `168.5 h`）。
- **負金額以帳務括號＋紅字**呈現（如 `($2,000)`），標示不應出現的負值。

## [1.2.1] - 2026-07-07

### 修正
- 唯讀逐欄編輯欄位（`EditableField`）**取消底線**，改以輸入框外框呈現可編輯。
- **數字格式區分**：金額＝整數千分位（45,000）、一般數字＝千分位並保留小數（168.5）、比率＝百分比（6.0%）。

## [1.2.0] - 2026-07-07

### 新增
- **表單「唯讀逐欄編輯」基礎**（`EditableField` / `ChangeSummary` / `diff`）：編輯資料時欄位預設唯讀（呈現為底線的輸入框樣式），點欄位才變輸入；值一改就整欄標色＋「已變更」標籤＋右側 undo 鈕還原；送出前列出「舊→新」變更摘要，可逐欄或全部還原。
- **基本資料（員工檔案）導入為範本**：三分頁純量欄位改為唯讀逐欄編輯＋送出前變更摘要；新增模式欄位直接可輸入；當期已確認的薪資相關欄位顯示鎖定。（其餘表單將於後續版本陸續導入。）

### 開發
- 導入 **Storybook 8**：全元件（基礎／表單／圖表）與 13 個整頁畫面 stories，供元件檢視與整體畫面優化討論（`npm run storybook`）。

## [1.1.1] - 2026-07-07

### 修正
- **行動版選單改為左側滑出抽屜**（含背景遮罩、關閉鈕）：不再內嵌於畫面上方擠壓內容，切換頁面免捲回最上方。
- **彈出視窗改為自畫面中央縮放淡入**：修正動畫因缺少 `slide-*-1/2` 位移類別而從右下角彈出的問題；行動版改為留邊置中卡片（圓角）。

## [1.1.0] - 2026-07-07

### 新增
- **系統版本顯示**：側邊欄常駐版號（`v1.1.0 · <sha>`，可點進 GitHub commit）、系統設定「關於」卡（版號／commit／建置時間台北時區／參數年度）、報表與薪資條列印頁尾版號。版號由 `package.json` 與 build 時注入的 SHA/時間組成。
- **員工生命週期**：新增「停職」「暫離」狀態與「復職」概念；以生效日＋復職日區間逐日精算破月（各狀態給薪比例，公司政策預設＋逐案覆寫）；到／離職當月勞保費按在職天數計、健保與勞退整月。
- **加退保名冊**：擴為加保／退保／停保／復保 4 類，並在本月工作台加待辦。
- **固定津貼**：可自訂命名新增津貼（計入月薪資總額／級距／加班）；公司可設「新進員工預設」於建檔時帶入；每月薪資作業加動線指引指向基本資料。

### 變更
- 分析頁 CSV 匯出補齊、圖表卡收斂 ChartCard、投保級距表遷 DataTable。
- 驗證 V8 擴充：復職日早於生效日時報錯。
- 示範公司擴為 62 人（含停職半薪、復職邊界個案）。

### CI
- `deploy-pages.yml` 新增 `release` job：發佈到 main 時依 `package.json` 版號自動建立 `vX.Y.Z` tag 與 GitHub Release（已存在則略過）。

### 文件
- README 頂部加線上 Demo 連結並更新過時的功能描述；附錄 C／CLAUDE.md 同步更新。

## [1.0.0] - 2026-07-02

- 首個正式版：依 README 規格的台灣 115 年（2026）合規薪資計算（投保級距、四險保費、加班、請假、所得稅扣繳、二代補充保費、破月）；62 人示範公司開箱預設；每月結算兩步流程、報表與申報中心、薪酬分析、專案人事成本、出勤打卡、整檔備份還原、稽核軌跡。以 TC-1～TC-9 驗收（`tests/acceptance.test.ts`）。
