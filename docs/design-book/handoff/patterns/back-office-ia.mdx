---
title: 後台系統的資訊架構
---

{/*
  草案：新頁 → book/docs/4-patterns/11-back-office-ia.mdx
  為什麼上游需要這一頁：現有 10 則模式都是「一個畫面之內」的模式
  （欄位怎麼編、表格怎麼做、確認怎麼鎖）。整個系統的骨架——
  側邊欄怎麼分區、同一件事有幾個入口、待辦怎麼跳到目標——反而沒有頁面講。
  而這是使用者第一天就會遇到、且事後最難改的一層：改分區＝改所有人的肌肉記憶。
  本頁不使用 <Demo>：側邊欄與路由是應用外殼，不是元件庫的元件，
  用結構圖與表格陳述比放一個假的側邊欄誠實。
*/}

# 後台系統的資訊架構

## 問題

後台系統的側邊欄，預設會長成**資料表的目錄**：

```
訂單
客戶
品項
倉庫
工單
報表
設定
```

每一項都對應一張資料表。開發的人覺得完整又整齊，使用者卻找不到東西——
因為使用者要做的不是「看一張表」，是**完成一件事**，
而一件事幾乎不會只碰一張表。

「這個月的結算」要碰訂單、品項、報表；「新客戶開通」要碰客戶、設定、工單。
於是使用者的日常變成在側邊欄之間反覆跳，而每一次跳都要重新想「這一步在哪裡」。

更糟的是同一件事有好幾個入口：結算流程裡有一個「匯出報表」，
側邊欄也有一個「報表」，兩邊做的事差不多但不完全一樣。
使用者會問「哪一個才對」，而正確答案是「都對，但你要記得哪個場合用哪個」——
這不是一個系統該要求使用者記住的事。

## 做法：依工作節奏分區

側邊欄的分區不對應資料表，對應**使用者的工作節奏**：

```
每日作業        ← 天天做、有時間壓力
  工作台
  訂單結算            〔例行〕
  庫存盤點

規劃與分析      ← 想事情的時候才進來、不影響正式資料
  成本分析            〔試算〕

報表            ← 對外交付
  報表中心            〔例行〕

主檔與設定      ← 偶爾改、影響全公司
  基本資料
  系統設定

說明            ← 卡住的時候
  使用說明
  操作手冊（外連）
```

同樣是七、八個項目，但這個排法回答的是**「我現在要做的事在哪一區」**，
而不是「我要找的資料存在哪張表」。

分區的判準是頻率與後果，不是資料模型：

| 分區 | 頻率 | 改壞了會怎樣 |
| --- | --- | --- |
| 每日作業 | 每天 | 當天的作業卡住 |
| 規劃與分析 | 每週／每月 | 不會怎樣（沙盒） |
| 報表 | 每期 | 對外文件錯誤 |
| 主檔與設定 | 每季 | **全公司的計算結果改變** |

<Rules>
  <Do><p>把<strong>影響半徑最大</strong>的東西（主檔、設定）放在最下面、最不順手的位置。
  順手程度應該與「按錯的代價」成反比。</p></Do>
  <Dont><p>不要把設定放在最上面只因為它字母排序在前，
  也不要因為「這一區只有一項」就把它併進別區。分區的數量不是成本，
  <strong>分錯區才是</strong>。</p></Dont>
</Rules>

這個分法在別的領域一樣成立，因為它問的是同一組問題：

| 系統 | 每日作業 | 規劃與分析 | 主檔與設定 |
| --- | --- | --- | --- |
| 客戶關係管理 | 今日跟進、待回覆 | 漏斗預測（沙盒） | 客戶主檔、階段定義 |
| 工單系統 | 今日派工、逾期單 | 產能模擬 | 服務類型、工時費率 |
| 庫存系統 | 收發貨、盤點 | 補貨模擬 | 品項主檔、倉別 |

## 「例行」與「試算」標籤

在項目旁掛一個小標籤，說明這個畫面**動不動到正式資料**：

| 標籤 | 意思 |
| --- | --- |
| 〔例行〕 | 正式作業。這裡做的事會進入正式紀錄 |
| 〔試算〕 | 沙盒。隨便改，不會影響任何正式資料 |

為什麼值得為此多做一個標籤：**沙盒的價值來自使用者相信它是沙盒**。
一個不確定會不會弄壞東西的模擬工具，使用者不會拿它來模擬——
他會改到一半就退出去，然後用試算表算。

反過來也一樣：正式作業畫面要讓人知道自己在正式環境裡，
這是[硬鎖定](/patterns/hard-lock)那條「刻意的決定」在導覽層的延伸。

<Rules>
  <Do><p>試算畫面內部再標一次<strong>資料狀態徽章</strong>
  （「試算暫定」／「已確認實際」／「歷史實際」）。導覽層說的是這個畫面的性質，
  徽章說的是<strong>眼前這組數字</strong>的性質。</p></Do>
  <Dont><p>不要讓試算畫面有任何一顆按鈕會靜靜寫入正式資料。
  真的需要「把試算結果套用出去」，那顆按鈕要獨立、要說清楚後果、
  而且要是整個畫面上唯一一個。</p></Dont>
</Rules>

## 全域健康度狀態列

頂部固定一條狀態列，隨時顯示**兩件事**：

1. **待處理項目數**（驗證器即時算出的錯誤數）
2. **目前作業期間的狀態**（已確認／未確認）

它的作用不是通知，是**定位**。後台使用者常常在五、六個畫面之間來回，
狀態列讓他在任何一頁都知道「這一期還能不能收」「還有沒有東西沒處理」，
不必回到工作台才看得到。

<Rules>
  <Do><p>數字要<strong>可點</strong>，點下去直接到待處理清單。
  一個說「還有 3 項」卻不告訴你是哪 3 項的指示器，只會製造焦慮。</p></Do>
  <Dont><p>不要在狀態列放「一切正常」以外的常駐訊息。
  常駐的東西一旦超過兩項，全部都會變成背景，包括真正重要的那一項。</p></Dont>
</Rules>

## 單一出口：同一件事只有一個入口

「產出對外文件」這件事**只有一個地方做得到**，其餘位置一律連過去。

舊入口不刪，改成**轉址**：

```
/flow/export   →  redirect  →  /reports
/export        →  redirect  →  /reports
```

為什麼是轉址而不是刪除：使用者收藏過網址、文件寫過網址、同事之間傳過網址。
直接刪掉會得到 404，而 404 對使用者的意思是「這個功能被拿掉了」——
他不會猜到功能還在、只是搬家了。

<Rules>
  <Do><p>合併多個入口時，把<strong>各入口原本能做的事全部確認過</strong>一遍。
  「差不多的兩個入口」通常有一個做得到某件另一個做不到的事，
  那件事就是使用者當初選擇走那條路的原因。</p></Do>
  <Dont><p>不要保留「兩個入口，功能 90% 重疊」。使用者遲早會發現兩邊算出來的數字不一樣，
  然後就再也不相信任何一邊。多入口的真正代價不是混淆，是<strong>不一致</strong>。</p></Dont>
</Rules>

## 深連結：讓待辦能直接跳到目標

畫面內的**分頁**與**選中的對象**要寫進網址：

```
/master?tab=records&id=C-1043
```

沒有這個的話，工作台上的待辦事項只能寫「請到基本資料 → 異動紀錄 → 找到 C-1043」，
使用者要自己走三步；有了之後，待辦本身就是一個連結。

連帶得到三件事：可收藏、可貼給同事、瀏覽器上一頁行為正確
（分頁切換算不算一次導覽？寫進網址之後這個問題就有答案了）。

<Rules>
  <Do><p>網址參數用<strong>使用者看得懂的字</strong>（<code>?tab=records</code>），
  不要用內部索引（<code>?t=2</code>）。網址是會被貼進聊天室的，第 2 個分頁是哪個沒有人記得。</p></Do>
  <Dont><p>不要把「哪一列被展開」「捲軸位置」這種暫時狀態也寫進網址。
  判準是：<strong>別人打開這個連結時，需要看到一樣的東西嗎？</strong> 不需要就別寫。</p></Dont>
</Rules>

## 行動版：側邊欄改抽屜

窄螢幕下側邊欄收成左滑抽屜（點遮罩關閉），**分區與順序完全不變**。

不要為手機重新設計一套導覽。後台系統的手機使用者多半是**同一個人在通勤時查東西**，
他腦中的地圖是桌機版那一張；換一套結構等於要他學第二遍。

## 取捨

**代價一：分區沒有標準答案，而且改起來很痛。**

側邊欄是使用者肌肉記憶的所在地。上線半年後把「報表」從第二區搬到第四區，
會收到「東西不見了」的回報——即使它明明還在。

所以分區要在**還沒有人熟悉之前**定案，並且盡量用「頻率與後果」這種
不隨功能增減而變動的判準，而不是用當下的功能清單去湊。

**代價二：依工作節奏分區，代表同一張資料表可能出現在兩區。**

主檔在「主檔與設定」可以完整維護，同一筆資料在「每日作業」也可能被就地編輯。
這是刻意的——使用者在作業流程中發現要改一個欄位時，
不該被要求離開流程、去另一區、改完再回來。

要付的代價是：**這兩個地方的行為必須一致**（同樣的鎖定規則、同樣的稽核紀錄）。
做不到一致的話，寧可只留主檔那一個入口。

## 反例

<Rules>
  <Do><p>側邊欄的每一項都要是<strong>使用者會說出口的詞</strong>。</p></Do>
  <Dont><p>不要用系統內部的模組名當導覽標籤。
  「基礎資料維護模組」不是任何人會用來描述自己在做什麼的講法。</p></Dont>
</Rules>

<Rules>
  <Do><p>未知路徑 fallback 到<strong>最常用的作業畫面</strong>。</p></Do>
  <Dont><p>不要 fallback 到首頁然後什麼都不說，更不要顯示空白。
  使用者手動改網址失敗時，最需要的資訊是「你在哪、可以去哪」。</p></Dont>
</Rules>
